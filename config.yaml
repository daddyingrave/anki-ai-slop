# New configuration schema
# Pipelines configuration
pipelines:
  obsidian_to_anki:
    # Discovery
    vault_dir: /Users/daddyingrave/Documents/default
    notes_path: Clippings/Heaps.md

    generate:
      model: models/gemini-2.0-flash-lite
      temperature: 0.2
      max_retries: 2
      backoff_initial_seconds: 0.5
      backoff_multiplier: 2.0
    review:
      model: models/gemini-2.0-flash-lite
      temperature: 0.1
      max_retries: 2
      backoff_initial_seconds: 0.5
      backoff_multiplier: 2.0
